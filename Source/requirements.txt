# Версии зафиксированы по факту установки, а не по памяти.
# Обновлять осознанно: langgraph и langchain ломают API между мажорами.

# ── Telegram (бот для медиков) ────────────────────────────────────────────────
aiogram==3.30.0

# ── Агент ─────────────────────────────────────────────────────────────────────
langgraph==1.2.11
langgraph-checkpoint-postgres==3.1.2   # состояние диалога переживает рестарт
langchain-openai==1.5.1
openai==3.1.0

# ── Веб-платформа (кабинет клиники) ───────────────────────────────────────────
fastapi==0.141.1
uvicorn[standard]==0.52.3
jinja2==3.1.6                          # серверный рендеринг, без сборки фронтенда
python-multipart==0.0.32               # формы и загрузка аудио из браузера
itsdangerous==2.2.0                    # подпись сессионной cookie
argon2-cffi==25.1.0                    # хэш пароля клиники

# ── База ──────────────────────────────────────────────────────────────────────
psycopg[binary,pool]==3.2.10
psycopg-pool==3.3.1

# ── Конфиг и валидация ────────────────────────────────────────────────────────
pydantic==2.13.4
pydantic-settings==2.15.0
python-dotenv==1.1.1
email-validator==2.3.0                 # нужен pydantic.EmailStr в форме входа

# ── Тесты ─────────────────────────────────────────────────────────────────────
pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.28.1                          # тесты HTTP-слоя ходят по приложению через ASGI

# ── QR ───────────────────────────────────────────────────────────────────────
segno==1.6.6                           # генерация QR: чистый python, без Pillow

# ── Качество кода ─────────────────────────────────────────────────────────────
ruff==0.16.3
